<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>Password Reset | Tri-Link Car Rentals</title>
    <style>
      @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap');
      
      body {
        font-family: 'Poppins', Arial, sans-serif;
        background: #f8f6fa;
        color: #351243;
        margin: 0;
        padding: 0;
      }
      .email-container {
        max-width: 600px;
        margin: 32px auto;
        background: #fff;
        border-radius: 16px;
        box-shadow: 0 4px 24px rgba(134, 113, 142, 0.2);
        overflow: hidden;
      }
      .email-header {
        background: linear-gradient(90deg,#351243,#86718e);
        padding: 24px;
        text-align: center;
      }
      .email-header h1 {
        color: white;
        margin: 0;
        font-size: 24px;
      }
      .email-content {
        padding: 32px;
      }
      h2 {
        color: #351243;
        margin-top: 0;
      }
      .reset-btn {
        display: inline-block;
        background: linear-gradient(90deg,#351243,#86718e);
        color: #fff;
        padding: 12px 32px;
        border-radius: 8px;
        text-decoration: none;
        font-weight: 600;
        font-size: 16px;
        box-shadow: 0 4px 12px rgba(134, 113, 142, 0.3);
      }
      .token-box {
        background: #f8f6fa;
        border-radius: 8px;
        padding: 16px;
        margin: 24px 0;
        font-size: 14px;
      }
      .token-code {
        background: white;
        padding: 12px;
        border-radius: 4px;
        font-family: monospace;
        word-break: break-all;
      }
      .email-footer {
        border-top: 1px solid #eee;
        margin-top: 32px;
        padding-top: 16px;
        color: #86718e;
        font-size: 14px;
      }
    </style>
  </head>
  <body>
    <div class="email-container">
      <!-- Header with Branding -->
      <div class="email-header">
        <h1>Tri-Link Car Rentals</h1>
      </div>
      
      <!-- Content -->
      <div class="email-content">
        <h2>Password Reset Request</h2>
        <p>Hello <strong><%= name %></strong>,</p>
        <p>We received a request to reset your Tri-Link Car Rentals account password. Click the button below to set a new password. This link will expire in <strong><%= expiresIn %></strong>.</p>
        
        <!-- Reset Button -->
        <div style="text-align: center; margin: 32px 0;">
          <a href="http://localhost:4200/auth/reset-password?token=<%= token %>" class="reset-btn">
            Reset Password
          </a>
        </div>
        
        <!-- Alternative Token Method -->
        <div class="token-box">
          <p style="margin-top: 0;">Or reset your password using this token:</p>
          <div class="token-code"><%= token %></div>
          <p style="margin-bottom: 0;">Enter this token on the password reset page at <a href="http://localhost:4200/auth/reset-password" style="color: #86718e;">http://localhost:4200/auth/reset-password</a></p>
        </div>
        
        <p>If you didn't request this password reset, please ignore this email or contact support if you have concerns.</p>
        
        <div class="email-footer">
          <p style="margin: 8px 0;">For your security,